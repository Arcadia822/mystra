// Dependency-safe base module: Session message bounds shared by the Session
// contracts and the Task production contracts, which cannot import each other.
export const SESSION_TEXT_MAX_LENGTH = 64 * 1024;
