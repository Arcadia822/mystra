---
title: "059 需求质量检查"
taco_scope: "spec"
---

## 检查

- [x] 命名操作者、调用者、Runner维护者，使用可独立验证的技术场景。
- [x] 用户已裁定文档工作负载、可控启动指令、常驻代理和独立审查要求。
- [x] 明确修订旧规范全任务强制代码/PR语义，保留单一平台标准提示词和系统优先级。
- [x] 正常鉴权、幂等、延后启动、能力隔离、真实恢复和文件验收均有场景。
- [x] 范围不含OpenSandbox、通用workflow、Artifact产品、外部凭据代理或UI新控件。
- [x] SDK恢复的不确定性已通过独立源码研究和真实两轮实验缩小；正式集成仍须验证。
- [x] 成功标准区分Session状态与实际产物，禁止把ready当成完成。
- [x] 沙箱最小权限有可验证合同：模型凭据只存在ephemeral挂载并在首个caller prompt前删除，仓库扩展不执行，guest出口默认拒绝且只放行模型端点；实现与实测见`research.md`。

## 需求评分

自评94/100：目标29/30，功能24/25，操作者流程18/20，技术约束14/15，范围9/10。需求可进入实施；这不是实现或部署通过的评分。

剩余运行前提：正常用户登录；正式代码整合后的数据库与Provider验证；真实Task的文档产出及续接。上述记录为验收依赖，不用伪造会话或改为独立脚本冒充完整链。

## 独立设计审查

LaunchContractReview第二次审查确认启动指令合同及单一中立系统提示词成立。实现阻断项已进入tasks：SDK依赖锁定、绝对导入、明确sessionId、stopReason、能力失败关闭、只读CLI和最小binding环境。代理复核无阻断问题。开发完成后仍需独立代码审查。
