{
  "name": "@mystra/workflows",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "lint": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mystra/shared": "workspace:*",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
