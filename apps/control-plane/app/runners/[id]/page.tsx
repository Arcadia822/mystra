import { EmptyState } from "../../_components/states";

export default function RunnerDetailPage() {
  return <div className="pageContent"><EmptyState title="Runner detail is temporarily unavailable" description="Runner persistence and management APIs are outside the active Prisma schema." /></div>;
}
